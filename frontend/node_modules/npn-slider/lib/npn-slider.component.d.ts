import { EventEmitter, ElementRef, OnInit, SimpleChanges, OnChanges } from '@angular/core';
import { Utilities } from './utilities';
import { SliderHandlerEnum } from './slider-handler.enum';
export declare class NpnSliderComponent extends Utilities implements OnInit, OnChanges {
    private el;
    private sliderModel;
    private step;
    private sliderWidth;
    private totalDiff;
    private startClientX;
    private startPleft;
    private startPRight;
    private minValue;
    private maxValue;
    private minSelected;
    private maxSelected;
    private sliderInitiated;
    initValues: number[];
    currentValues: number[];
    handlerX: number[];
    isHandlerActive: boolean;
    isTouchEventStart: boolean;
    isMouseEventStart: boolean;
    currentHandlerIndex: number;
    stepIndicatorPositions: any[];
    isDisabled: boolean;
    hideTooltip: boolean;
    hideValues: boolean;
    handlerIndex: typeof SliderHandlerEnum;
    constructor(el: ElementRef);
    setMinValues: number;
    setMaxValues: number;
    setMinSelectedValues: number;
    setMaxSelectedValues: number;
    stepValue: number;
    showStepIndicator: boolean;
    multiRange: boolean;
    setHideTooltip: boolean;
    setHideValues: boolean;
    setDisabled: string;
    onChange: EventEmitter<number[]>;
    ngOnInit(): void;
    ngOnChanges(changes: SimpleChanges): void;
    initializeSlider(): void;
    private resetModel();
    private validateSliderValues();
    private initializeStepIndicator();
    private updateCurrentValue(arrayValue, privateChange?);
    private updateInitValues(arrayValue);
    private setHandlerPosition();
    private setModelValue(index, value);
    setHandlerActiveOff(): void;
    setHandlerActive(event: any, handlerIndex: number): void;
    handlerSliding(event: any): void;
}
